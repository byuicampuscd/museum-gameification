// link to this file in html & use it in courseData to parse gradebook
const settings = {
    overall: new RegExp(/:o/),
    badge: new RegExp(/:b/), // pass-off
    passPercent: .7,
    preparation: new RegExp(/:r/), // prep
    elective: new RegExp(/:e/) // unused

}
