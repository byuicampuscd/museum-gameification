// link to this file in html & use it in courseData to parse gradebook
const settings = {
    overall: new RegExp(':o'),
    badge: new RegExp(':b'), // or pass-off
    preparation: new RegExp(':r'), //or prep
    elective: new RegExp(':e')
}
